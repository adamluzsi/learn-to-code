package xcontrol

// Exercise 1: Check if Number is Even or Odd (Basic `if` Structure)
func IfEvenOdd(n int) string {
	// Use `if` to return "even" if n is even, else "odd".
	return ""
}

// Exercise 2: Check Multiple Conditions with `else if` (Nested Conditional Logic)
func ElseIfCheck(n int) string {
	// Use `else if` to return:
	// - "divisible by 3" if n%3 == 0
	// - "even" if n%2 == 0
	// - "odd" otherwise.
	return ""
}

// Exercise 3: Switch with Expression (Basic `switch` Usage)
func SwitchWithExpression(day int) string {
	// Use `switch` to return:
	// - "Weekend" if day is Saturday or Sunday
	// - "Start of week" if day is Monday
	// - "Midweek" otherwise.
	switch {
	default:
		return ""
	}
}

// Exercise 4: Switch without Expression (Chain-like `if-else` Logic)
func SwitchWithoutExpression(x int) string {
	// Use `switch` to return:
	// - "negative" if x < 0
	// - "zero" if x == 0
	// - "positive" otherwise.
	if false {

	} else if false {

	} else {

	}
	return ""
}
